import { fn } from './index'

// TODO：增加 react hooks 的单测依赖
// TODO：增加 react hooks 的单测依赖
// TODO：增加 react hooks 的单测依赖
// TODO：增加 react hooks 的单测依赖
describe('xxx', () => {
	test('两数相加', () => {
		expect(fn(1, 2)).toBe(3)
	})

	it('ffff', () => {
		expect(fn(2, 2)).toBe(4)
	})
})
